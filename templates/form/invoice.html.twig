<div class="form-group">
        <div class="row">
                <div class="col-sm">
                        {{ form_label(childForm.invoiceToReceiver) }}
                        {{ form_widget(childForm.invoiceToReceiver) }}
                </div>
                <div class="col-sm">
                    {{ form_label(childForm.customerNumberReceiver) }}
                    {{ form_widget(childForm.customerNumberReceiver, { 'value': '' }) }}
                </div>
        </div>
</div>

